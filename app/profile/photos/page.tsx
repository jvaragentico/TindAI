import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import PhotoManager from"./photo-manager";export default async function Photos(){const s=await createClient();const{data}=await s.auth.getClaims();if(!data?.claims)redirect("/auth/login");const uid=String(data.claims.sub);const{data:photos}=await s.from("profile_photos").select("id,storage_path,position").eq("user_id",uid).order("position");return <main className="shell center"><section className="authBox"><div className="brand">tind<span className="ai">AI</span></div><p className="eyebrow">YOUR LOOK</p><h1>Add up to six photos.</h1><p className="lead">Preview before uploading. Your first photo becomes your primary discovery image.</p><PhotoManager uid={uid} initial={photos||[]}/></section></main>}
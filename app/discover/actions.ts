"use server";import{revalidatePath}from"next/cache";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";export async function decide(fd:FormData){const s=await createClient();const{data}=await s.auth.getClaims();if(!data?.claims)redirect("/auth/login");const target=String(fd.get("target")||"");const decision=String(fd.get("decision")||"");const{data:matched,error}=await s.rpc("record_decision",{p_target:target,p_decision:decision});if(error)redirect("/discover?error="+encodeURIComponent(error.message));revalidatePath("/discover");revalidatePath("/matches");if(matched)redirect("/matches?new=1")}
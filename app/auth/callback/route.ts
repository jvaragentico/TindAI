import{NextResponse}from"next/server";import{createClient}from"@/lib/supabase/server";const SITE="https://tindai-topaz.vercel.app";export async function GET(request:Request){const{searchParams}=new URL(request.url);const code=searchParams.get("code");if(!code)return NextResponse.redirect(SITE+"/auth/login?error="+encodeURIComponent("Invalid or expired confirmation link"));const s=await createClient();const{error}=await s.auth.exchangeCodeForSession(code);if(error)return NextResponse.redirect(SITE+"/auth/login?error="+encodeURIComponent(error.message));return NextResponse.redirect(SITE+"/profile/setup")}
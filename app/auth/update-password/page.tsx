"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{createClient}from"@/lib/supabase/client";export default function UpdatePassword(){const[p,setP]=useState("");const[msg,setMsg]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();if(p.length<8){setMsg("Use at least 8 characters.");return}const{error}=await createClient().auth.updateUser({password:p});if(error){setMsg(error.message);return}router.replace("/")}return <main className="shell center"><section className="authBox"><div className="brand">tind<span className="ai">AI</span></div><p className="eyebrow">NEW CREDENTIALS</p><h1>Choose a new password.</h1>{msg&&<div className="error">{msg}</div>}<form onSubmit={submit}><label>New password<input value={p} onChange={e=>setP(e.target.value)} type="password" minLength={8} required autoComplete="new-password"/></label><button className="cta">Update password</button></form></section></main>}
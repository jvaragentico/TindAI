import Link from"next/link";import{login}from"../actions";export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return <main className="shell center"><section className="authBox"><div className="brand">tind<span className="ai">AI</span></div><p className="eyebrow">WELCOME BACK, HUMAN</p><h1>Return to the experiment.</h1>{error&&<div className="error">{error}</div>}<form action={login}><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label><button className="cta" type="submit">Log in</button></form><Link className="textLink" href="/auth/forgot-password">Forgot password?</Link><p className="switch">New signal? <Link href="/auth/signup">Create account</Link></p></section></main>}
import Link from"next/link";import{signup}from"../actions";export default async function Signup({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return <main className="shell center"><section className="authBox"><div className="brand">tind<span className="ai">AI</span></div><p className="eyebrow">JOIN THE EXPERIMENT</p><h1>Create your human profile.</h1><p className="lead">You must be 18 or older. Some participants are synthetic characters and will be revealed after each challenge.</p>{error&&<div className="error">{error}</div>}<form action={signup}><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" minLength={8} required autoComplete="new-password"/></label><label className="check"><input name="age" type="checkbox" required/> I confirm I am 18+ and understand synthetic participants are part of TindAI.</label><button className="cta" type="submit">Create account</button></form><p className="switch">Already joined? <Link href="/auth/login">Log in</Link></p></section></main>}
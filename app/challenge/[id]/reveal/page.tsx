import Link from"next/link";import{notFound,redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";export default async function Reveal({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await createClient();const{data}=await s.auth.getClaims();if(!data?.claims)redirect("/auth/login");const{data:r,error}=await s.rpc("get_reveal",{p_conversation:id});if(error||!r?.[0])notFound();const x=r[0],isAI=x.participant_type==="synthetic_agent";return <main className="shell center"><section className="revealCard"><p className="eyebrow">SIGNAL REVEALED</p><div className="revealIcon">{isAI?"AI":"H"}</div><h1>{isAI?"You met an AI.":"You met a human signal."}</h1><p>{isAI?"This participant is a fictional synthetic TindAI character.":"For this experiment, simulated-human profiles count as the human side of the challenge."}</p><div className={x.correct?"result correct":"result"}>{x.correct?"✓ Your instinct was right.":"✦ You were fooled."}</div><Link className="cta" href="/discover">Try another signal</Link></section></main>}
import Link from"next/link";import{notFound,redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";export default async function Reveal({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await createClient();const{data}=await s.auth.getClaims();if(!data?.claims)redirect("/auth/login");const{data:r,error}=await s.rpc("get_reveal",{p_conversation:id});if(error||!r?.[0])notFound();const x=r[0],isAI=x.participant_type==="synthetic_agent",right=Boolean(x.correct);return <main className="shell center"><section className="revealCard revealCentered"><div className="revealIcon">{right?"🫶":"🤪"}</div><h1>{right?(isAI?"🫶 You’re right! It was AI.":"🫶 You’re right! She is real 💞"):(isAI?"🤪 Sorry, she doesn’t exist ❌👎":"🤪 Sorry, she is real 💞")}</h1><p>{isAI?"This was a fictional synthetic TindAI participant.":"This was the human side of the TindAI challenge."}</p><div className={right?"result correct":"result"}>{right?"Great intuition — you made the right call.":"That one fooled you. Ready for another?"}</div><Link className="cta" href="/discover">{right?"Return to discovery":"Retry"}</Link></section></main>}
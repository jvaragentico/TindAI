"use client";import{useEffect,useMemo,useRef,useState}from"react";import{createClient}from"@/lib/supabase/client";import{guess}from"../actions";type M={id:number;sender_profile_id:string;body:string;created_at:string};export default function ChatRoom({conversation,target,myProfileId,initial,justMatched=false}:{conversation:any,target:any,myProfileId:string,initial:M[],justMatched?:boolean}){const s=useMemo(()=>createClient(),[]),[messages,setMessages]=useState(initial),[body,setBody]=useState(""),[now,setNow]=useState(Date.now()),[status,setStatus]=useState(conversation.status),[sending,setSending]=useState(false),[chatError,setChatError]=useState(""),bottom=useRef<HTMLDivElement>(null),[showMatch,setShowMatch]=useState(justMatched),end=new Date(conversation.expires_at).getTime(),left=Math.max(0,Math.ceil((end-now)/1000));useEffect(()=>{const t=setInterval(()=>setNow(Date.now()),250);return()=>clearInterval(t)},[]);useEffect(()=>{if(left===0&&status==="active"){setStatus("guessing");s.rpc("expire_challenge",{p_conversation:conversation.id})}},[left,status,s,conversation.id]);useEffect(()=>{const ch=s.channel("chat:"+conversation.id).on("postgres_changes",{event:"INSERT",schema:"public",table:"messages",filter:`conversation_id=eq.${conversation.id}`},p=>{const m=p.new as M;setMessages(x=>x.some(y=>y.id===m.id)?x:[...x,m])}).subscribe();return()=>{s.removeChannel(ch)}},[conversation.id,s]);useEffect(()=>bottom.current?.scrollIntoView({behavior:"smooth"}),[messages]);async function send(e:React.FormEvent){e.preventDefault();const text=body.trim();if(!text||left<=0||sending)return;setSending(true);setChatError("");const{data:u}=await s.auth.getUser();if(!u.user){setChatError("Your session expired. Please log in again.");setSending(false);return}const{data:inserted,error}=await s.from("messages").insert({conversation_id:conversation.id,sender_user_id:u.user.id,sender_profile_id:myProfileId,body:text}).select("id,sender_profile_id,body,created_at").single();if(error){setChatError("Message could not be sent. Try again.");setSending(false);return}setBody("");if(inserted)setMessages(x=>x.some(y=>y.id===inserted.id)?x:[...x,inserted as M]);try{const r=await fetch("/api/agent-reply",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({conversationId:conversation.id})});if(!r.ok){const j=await r.json().catch(()=>({}));setChatError(j.error==="AI server not configured"?"AI reply is not configured on this deployment.":"Your message was sent, but the reply failed. Try another message.")}}catch{setChatError("Your message was sent, but the reply failed.")}finally{setSending(false)}}const mm=String(Math.floor(left/60)).padStart(2,"0"),ss=String(left%60).padStart(2,"0");return <main className="challengeShell">{showMatch&&<div className="matchModal"><div className="matchBox"><div className="matchEmoji">🎉</div><h1>It’s a Match!</h1><p>But is {target.display_name} real or AI?</p><strong>You have 5 minutes to find out.</strong><button onClick={()=>setShowMatch(false)}>Start chatting</button></div></div>}<header className="challengeHead"><div><p className="eyebrow">5-MINUTE CHALLENGE</p><h2>{target.display_name}, {target.age}</h2></div><div className={left<30?"timer danger":"timer"}>{mm}:{ss}</div></header><div className="ruleStrip">No asking directly “Are you AI/human?” · No outside contact · Trust your instincts.</div><section className="chatStream">{messages.length===0&&<div className="chatIntro">The clock is running. Say something worth remembering.</div>}{messages.map(m=><div key={m.id} className={m.sender_profile_id===myProfileId?"bubble mine":"bubble theirs"}>{m.body}</div>)}{sending&&<div className="bubble theirs typing">Thinking…</div>}<div ref={bottom}/></section>{chatError&&<div className="chatError">{chatError}</div>}{left>0?<form className="composer" onSubmit={send}><input value={body} maxLength={1200} onChange={e=>setBody(e.target.value)} placeholder="Message…" disabled={sending}/><button disabled={!body.trim()||sending}>{sending?"…":"Send"}</button></form>:<section className="guessGate"><p className="eyebrow">TIME. MAKE THE CALL.</p><h1>Who were you talking to?</h1><form action={guess}><input type="hidden" name="conversation" value={conversation.id}/><button name="guess" value="human">HUMAN</button><button name="guess" value="ai">AI</button></form></section>}</main>}
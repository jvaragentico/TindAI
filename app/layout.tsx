import type { Metadata } from "next";import "./globals.css";export const metadata:Metadata={title:"TindAI — Human or AI?",description:"A five-minute social experiment. Match, talk, decide."};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}